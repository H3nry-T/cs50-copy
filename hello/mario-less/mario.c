#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n;
    do
    {
        h = get_int("Height: ");
    }
    while (h < 1 || h > 8);

    for(int row = 0; row < h; row++)
    {
        for(int column = 0; j < n; j++)
        {
        printf("#");
        printf("\n");
        }
    print("\n");
    }
}